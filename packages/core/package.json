{
  "name": "@sm-lab/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared internals for sm-lab — harvested from cl-mock & merkle during migration. Bundled into consumers, never published.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "catalog:",
    "@hono/node-server": "catalog:",
    "commander": "catalog:"
  },
  "devDependencies": {
    "@sm-lab/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@types/node": "catalog:"
  }
}
