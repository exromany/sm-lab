# Generated ABI modules — regenerated by `refresh` script; formatting would be lost on next run
fixtures/receipts/src/abi/*.ts

# Test fixture forge artifacts — verbatim copies of upstream build output
fixtures/receipts/test/fixtures/out/**/*.json

# Generated Prisma client — regenerated by prisma generate
tools/survey/src/generated/

# pnpm-generated lockfile — pnpm owns its format; prettier'd reflow reverts on next install
pnpm-lock.yaml

# Env files — no prettier parser; .env is gitignored, .env.sample is a plain KEY=value template
.env*

# Agent-generated planning docs — churned per skill session, not hand-maintained;
# formatting them just defers the next CI break. Hand-written docs/ (architecture,
# decisions) stay checked.
docs/superpowers/
