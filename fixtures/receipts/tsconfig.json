{
  "comment": "Relative extends, not the @sm-lab/config subpath — tsdown's Rust tsconfig loader doesn't follow package-exports extends. tsc here is typecheck-only (--noEmit).",
  "extends": "../../packages/config/tsconfig.lib.json",
  "include": ["src/**/*", "scripts/**/*", "test/**/*"]
}
