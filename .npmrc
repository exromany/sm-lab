# pnpm: link workspace packages by their declared version range, falling back to
# the workspace copy. Keeps `workspace:*` and `catalog:` resolution predictable.
link-workspace-packages=true
prefer-workspace-packages=true
# strict by default — no phantom dependencies. This is the main reason we picked pnpm.
auto-install-peers=true
