---
'@sm-lab/recipes': patch
---

`connect()` now forces anvil automine on so write→simulate recipes (e.g. install a gate tree, then
prove against it) see fresh state regardless of how the fork was launched.
